{
    "_comment_protocol": "PLC protocol selector. 'legacy_fronback' for existing toothpastefronback customers (uses original D0/D1/D2/D3/D4/D10-D11/D20-D23/D30-D36/D40 protocol — drop-in for sites with fixed PLC ladders). Omit or 'v2_unified' for new sites (per-camera ProductType in 18-word config blocks at D10..D27 / D30..D47).",
    "plc_protocol": "v2_unified",

    "_comment_cameras": "Per-camera entries are camera1, camera2, ... — add or remove freely (1-N supported). 'enabled': false skips the camera at startup. Hardware ROI (width/height multiples of 4) cuts payload and capture time. Algorithm-side ROI in PLC D25-27/D45-47 is then relative to this smaller frame.",
    "cameras": {
        "camera1": {
            "ip": "192.168.2.10",
            "host_lan": "192.168.2.123",
            "enabled": true
        },
        "camera2": {
            "ip": "192.168.3.10",
            "host_lan": "192.168.3.123",
            "enabled": true,
            "roi": {
                "width": 800,
                "height": 800,
                "offset_x": 240,
                "offset_y": 112
            }
        }
    },

    "plc": {
        "ip": "192.168.1.10"
    },

    "_comment_legacy_brush_head": "Defaults for legacy_fronback D2=2 (BRUSH_HEAD) mode. The PLC parameter block lives at D50-D63 — writing 0 to any slot means 'use these values'. Non-zero PLC writes override per cycle. adapt_C is not exposed via PLC (operator shouldn't tune the adaptive-threshold offset live); change it here if the algorithm starts misclassifying after a lighting change.",
    "legacy_brush_head_defaults": {
        "exposure": 5000,
        "shrink_pct": 15,
        "adapt_block": 31,
        "adapt_C": 8,
        "dot_area_min": 20,
        "dot_area_max": 500,
        "roi_area_min": 50000,
        "roi_area_max": 500000,
        "ratio_min": 1.5,
        "ratio_max": 3.5
    }
}
